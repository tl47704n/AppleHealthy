<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pc.applehealthy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EmailSignUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Search" />
        <activity android:name=".NewReminder" />

        <receiver android:name=".AlarmReceiver" />

        <activity android:name=".SetAlarm" />

        <service
            android:name=".RingtongPlayingService"
            android:enabled="true" />

        <activity android:name=".UploadName" />
        <activity android:name=".RealSearch" />
        <activity android:name=".SetOnlineAlarm" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>